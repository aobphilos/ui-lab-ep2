export enum StoneType {
  DIAMOND,
  COLORED_STONE,
  UNKNOWN,
}

export enum MountingType {
  LOOSE,
  MOUNTED,
}

export enum ReportType {
  PREMIUM_REPORT,
  REGULAR_REPORT,
  SMALL_REPORT,
  SEALING_CARD,
  SEALING_BOX,
}

export enum PickupType {
    IN_PERSON,
    KERRY,
}
